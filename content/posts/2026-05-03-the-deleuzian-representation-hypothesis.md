---
title: "The Deleuzian Representation Hypothesis"
date: 2026-05-03
draft: false
tags: [概念提取, 可解释性, 自监督学习, 多模态模型]
categories: [iclr-2026]
description: "概念提取 | 8.0/10"
hiddenInHomeList: true
---

# 📄 The Deleuzian Representation Hypothesis

#概念提取 #可解释性 #自监督学习 #多模态模型

🔥 **8.0/10** | 前25% | #概念提取 | #自监督学习 | #可解释性 #多模态模型

学术质量 6.0/7 | 选题价值 1.5/2 | 复现加成 0.5 | 置信度 高


### 👥 作者与机构

- 第一作者：Clément Cornet（Université Paris-Saclay, CEA, List）
- 通讯作者：未说明（三位作者均来自同一单位，无明确标注通讯作者）
- 作者列表：Clément Cornet（Université Paris-Saclay, CEA, List）、Romaric Besançon（Université Paris-Saclay, CEA, List）、Hervé Le Borgne（Université Paris-Saclay, CEA, List）

### 💡 毒舌点评

亮点：论文理论框架优雅，将哲学概念（德勒兹的差异论）与统计学习（判别分析）相结合，提出了一种概念简洁、物理意义明确（激活差异）的新范式；实验验证非常扎实，横跨视觉、语言、音频三大模态和五个模型，用近900个属性进行定量评估，远超同类工作的实验规模。短板：方法的核心假设——“概念方向是线性且各向同性分布”——可能过于理想化，对于神经网络内部高度非线性的表征结构，其普适性存疑；尽管跨模态验证了有效性，但“概念”的自动命名或语义理解仍需人工介入，限制了其全自动化分析的潜力。

### 🔗 开源详情

*   代码：提供了代码仓库链接：https://github.com/ClementCornet/Deleuzian-Hypothesis
*   模型权重：未提及。本方法分析的是已公开的预训练模型（CLIP, DinoV2, DeBERTa等），不需要训练新模型权重。
*   数据集：论文中使用的数据集（ImageNet, WikiArt, IMDB, CoNLL-2003, AudioSet）均为学术常用公开数据集，但论文未提供统一下载脚本或特别说明获取方式。
*   Demo：未提供在线演示。
*   复现材料：附录A详细描述了所有实验的实现细节，包括：概念数量（6144）、TopK-SAE的训练超参数（学习率1e-5, k=32）、各种SAE的L1惩罚系数选择过程、ICA的配置（FastICA, SVD白化）等。提供了全面的复现指南。
*   引用的开源项目：依赖scikit-learn（用于ICA和聚类），以及Hugging Face、PyTorch Hub上的预训练模型。

### 📌 核心摘要

1.  解决的问题：当前从神经网络中提取可解释概念的主流方法（如稀疏自编码器SAE）存在训练困难、可能产生多义特征、且过度依赖稀疏性作为可解释性代理等问题。
2.  方法核心：提出“德勒兹表示假设”，将概念建模为神经网络激活空间中的“差异方向”。具体方法为：随机采样样本对计算激活差异，然后对这些差异向量进行聚类（KMeans）以发现重复出现的模式，并利用激活分布的偏度对聚类进行加权以提升概念多样性。该过程可视为一种无监督的判别分析。
3.  与已有方法的相比新在哪里：与SAE等基于重建误差和稀疏约束的方法不同，本方法直接对样本间的差异进行建模和聚类，不试图重建原始激活。其理论基础与判别分析和德勒兹哲学中“概念作为差异”的观点对齐，且仅需一个可解释的超参数（概念数量k）。
4.  主要实验结果：
    *   概念质量：在20个任务中，本方法的探针损失（Probe Loss，越低越好）有13个任务达到最优，全局平均排名显著优于所有SAE变体。例如，在CLIP-WikiArt艺术家分类任务上，本方法探针损失为0.0055，优于最佳SAE变体TopK-SAE的0.0096。
    *   概念一致性：在最大成对皮尔逊相关（MPPC，越接近1越好）评估中，本方法的平均得分（如CLIP-ImNet为0.821）普遍较高，表明其提取的概念跨运行稳定性好。
    *   实际影响：概念引导实验证明，操控提取出的概念（如“国家”、“体育成就”）能直接、可逆地改变模型输出（如文本生成中的词替换、图像检索中的风格转移），展示了因果影响。
5.  实际意义：提供了一种更简单、更透明、概念质量更高的无监督概念提取新范式，可用于增强模型可信度、理解内部机制、进行模型干预（引导），并可能作为分析不同模型内部表征的通用工具。
6.  主要局限性：1）评估仍依赖有标签数据集，可能无法衡量与标签无关但有意义的“概念”；2）假设概念可线性表示，该假设可能在某些模型中不成立；3）概念的自动语义解释仍需人工完成。

### 🏗️ 模型架构

该方法并非传统意义上的神经网络架构，而是一个用于从预训练模型激活中提取概念的分析流程。其整体架构可概括为：采样差异 → 偏度加权聚类 → 概念向量输出。

1.  输入：预训练模型某一层的激活向量集合（N个样本，D维）。
2.  采样差异：随机配对样本（确保每个样本在减数和被减数中各出现一次），计算N个激活差异向量 `d_i = x_a - x_b`，构成差异集D。
3.  偏度计算与加权：对每个候选差异方向 `d_i`，计算所有样本在该方向上投影值的偏度 `μ₃(d_i)`。高偏度意味着该方向分布不均匀，易导致冗余聚类。因此，为每个差异向量分配权重 `w_i = 1/μ₃(d_i)`（对负偏度取反处理）。
4.  加权聚类：使用变体KMeans算法，对差异向量D进行聚类。聚类时，距离度量被权重调制：`d(d_i, C) = (1/μ₃(d_i)) * ||C - d_i||²`。这使得高偏度（可能冗余）的差异在聚类中被抑制。
5.  输出：聚类得到的k个质心向量，即为提取出的k个“概念向量”，它们是激活空间中的方向。

![论文中的图片](/audio-paper-digest-blog/images/iclr-2026/2026-05-03/10JEfJtiJM-1.png)
图2：该论文中的图片展示了本方法的整体流程。左侧为差异采样过程，中间为基于偏度的加权KMeans聚类，右侧为最终输出的作为概念向量的质心。该图清晰地阐释了从样本对差异到最终概念提取的完整数据流。

关键设计选择与动机：
*   操作于差异而非原始激活：动机在于德勒兹哲学认为概念源于差异。技术上，差异能天然地表示样本间的区分方向。
*   偏度加权：动机是解决标准聚类中少数高幅值、高偏度差异主导聚类、导致概念冗余的问题。
*   线性概念：基于高维空间中各向同性假设的判别分析（LDA）进行理论推导，简化了模型并提升了可解释性。论文附录G探讨了二次型扩展，但效果不佳。

![论文中的图片](/audio-paper-digest-blog/images/iclr-2026/2026-05-03/10JEfJtiJM-6.png)
图7：该论文中的图片展示了从CLIP模型提取的视觉概念示例。每张概念由9张激活值最高的图片表示，展示了如“小狗”、“水下生物”、“吉他”、“儿童肖像”、“悬崖绘画”、“威尼斯绘画”、“波普艺术”、“立体派绘画”、“极简主义绘画”等概念。这直观地证明了该方法能提取出语义多样的概念。

### 💡 核心创新点

1.  “差异即概念”的建模哲学与形式化：将德勒兹的哲学思想转化为具体算法，即通过对激活差异进行聚类来发现概念。与SAE的“重建即理解”范式形成鲜明对比，开辟了新路径。
2.  无监督判别分析框架：为差异聚类方法提供了坚实的统计学理论解释（公式2），将其置于判别分析框架下，增强了方法的理论深度和可信度。
3.  基于偏度的多样性增强机制：通过偏度加权巧妙解决了聚类中常见的模式坍缩和概念冗余问题，显著提升了提取概念的有效秩和多样性（见消融实验表3）。
4.  无损引导（Steering）能力：由于概念是激活空间中的直接向量，对模型表示的引导操作（`x' = x + αc`）是线性且可逆的，避免了SAE编码-解码过程中可能的信息损失。
5.  跨模态、大规模的统一评估：首次在视觉、语言、音频三个模态、五类模型、近900个属性上，使用探针损失和MPPC等定量指标，系统比较了多种概念提取方法，提供了迄今最全面的实证比较。

### 🔬 细节详述

*   训练数据：本方法本身是无监督的，仅需从预训练模型中提取某一层的激活值。但评估过程使用了带标签的数据集：ImageNet-100, WikiArt（含艺术家、风格、流派标签）, IMDB, CoNLL-2003（NER, POS, Chunking）, AudioSet。具体规模见论文表4。
*   损失函数：本方法在概念提取阶段没有训练损失，因为它是基于采样和聚类的非参数方法。评估时使用的探针损失（Probe Loss）是针对每个概念训练一个一维逻辑回归器来预测属性标签的交叉熵损失。
*   训练策略：不涉及传统梯度下降训练。核心步骤是：采样N对差异 → 计算每个差异方向在所有样本上的偏度 → 运行特征加权KMeans聚类。聚类是标准迭代过程。
*   关键超参数：概念数量 `k`，是唯一主要超参数，且具有明确语义（提取概念的个数）。论文中固定为6144（即8倍的激活维度）。
*   训练硬件：论文未说明。
*   推理细节：对于引导实验，直接进行向量加法 `x' = x + αc`。对于文本模型BART，引导发生在其编码器最后一层输出上，再送入解码器。
*   正则化技巧：通过偏度加权来正则化聚类过程，防止冗余。

### 📊 实验结果

论文在五大模型、三大模态上进行了广泛评估，关键结果如下：

主要对比实验：探针损失（Probe Loss，越低越好）

| 方法 | 类型 | CLIP (ImNet) | CLIP (WA-Artist) | DinoV2 (WA-Artist) | DeBERTa (IMDB) | DeBERTa (CoNLL-NER) | BART (CoNLL-NER) | AST (AudioSet) | Pythia (CoNLL-NER) | 平均排名↓ |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| LDA | 监督 | 0.0083 | 0.0044 | 0.0055 | 0.6394 | 0.0429 | 0.3473 | 0.0164 | 0.0742 | (参考) |
| ICA | 无监督 | 0.0154 | 0.0161 | 0.0137 | 0.6936 | 0.1251 | 0.6931 | 0.0234 | 0.1378 | 6.85 |
| Van-SAE | 无监督 | 0.0264 | 0.0220 | 0.0147 | 0.6893 | 0.0869 | 0.5983 | 0.0177 | 0.1498 | 4.65 |
| Tk-SAE | 无监督 | 0.0154 | 0.0096 | 0.0144 | 0.6858 | 0.0839 | 0.5980 | 0.0169 | 0.1321 | 2.65 |
| Deleuzian (Ours) | 无监督 | 0.0128 | 0.0055 | 0.0137 | 0.6849 | 0.0665 | 0.2148 | 0.0164 | 0.1121 | 1.65 |

表1（简化核心）：该论文中的表格是主要实验结果汇总。它显示，在探针损失指标上，本文提出的Deleuzian方法在多个任务（尤其是CLIP-WikiArt艺术家、DeBERTa-CoNLL NER、BART-CoNLL NER）上达到最优（粗体），且全局平均排名（Avg. Rank）显著低于所有无监督基线，接近监督方法LDA。

概念一致性实验：MPPC（越接近1越好）

| 方法 | CLIP (ImNet) | CLIP (WA) | DinoV2 (ImNet) | DinoV2 (WA) | DeBERTa (IMDB) | DeBERTa (CoNLL) | BART (IMDB) | BART (CoNLL) | AST (AS) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| Van-SAE | 0.840 | 0.918 | 0.603 | 0.903 | 0.986 | 0.437 | 0.996 | 0.439 | 0.837 |
| Tk-SAE | 0.757 | 0.861 | 0.588 | 0.824 | 0.866 | 0.594 | 0.996 | 0.761 | 0.601 |
| Deleuzian (Ours) | 0.821 | 0.856 | 0.789 | 0.843 | 0.980 | 0.588 | 1.0 | 0.768 | 0.830 |

表2（简化核心）：该论文中的表格展示了概念跨运行一致性。Deleuzian方法在大多数任务上MPPC值较高，表明其提取的概念稳定可重复。

消融实验关键结论（表3）
*   直接对原始激活进行聚类（`KMeans` on `acts.`）效果远差于对差异进行聚类（`KMeans` on `diff`），验证了“差异建模”的核心贡献。
*   在差异上应用偏度加权（`diff, KMeans, ✓`）比不加权（`diff, KMeans, ✗`）能显著提升概念的有效秩（如CLIP: 24.3 vs 124.4）并降低最大成对余弦相似度（如CLIP: 0.8685 vs 0.5677），证明了加权机制对提升多样性有效。

概念数量影响（图5）
![论文中的图片](/audio-paper-digest-blog/images/iclr-2026/2026-05-03/10JEfJtiJM-4.png)
图5：该论文中的图片展示了概念数量对性能的影响。即使只提取远少于6144的概念（如2000个），Deleuzian方法的探针损失仍能低于所有竞争方法，显示其高效的概念捕获能力。

### ⚖️ 评分理由

*   学术质量：6.0/7 - 创新性强：提出了一个完全不同于SAE的、具有清晰哲学与统计学理论基础的新范式。技术正确性高：方法描述清晰，推导严谨（附录G有扩展讨论），消融实验设计合理，充分验证了各组件的必要性。实验充分性极强：跨模态、多模型、大规模属性的定量评估，以及定性引导示例，构成了非常完整和有说服力的证据链。主要扣分点在于核心线性假设的普适性未被深入挑战或验证，且“概念”与“人类认知概念”的对齐度评估仍有主观成分。
*   选题价值：1.5/2 - 前沿性：位于机械可解释性研究的核心前沿，针对当前热点SAE方法提出有力替代方案。潜在影响：有望为理解、调试和可信控制神经网络提供更简单有效的工具。应用空间：可用于模型审计、安全对齐、故障诊断等。读者相关性：作为通用方法，其思想（差异建模、无损引导）对任何使用神经网络的领域（包括音频/语音）都有启发价值，但论文本身未专门探讨音频领域的特性。
*   开源与复现加成：+0.5/1 - 论文提供了代码仓库链接（https://github.com/ClementCornet/Deleuzian-Hypothesis），并在附录A中给出了详细的实现细节、超参数设置和基线配置。未提及模型权重（因为是分析预训练模型）和数据集的获取方式（但引用的数据集大多是公开的）。复现所需信息较为充分。

---

[← 返回 ICLR 2026 论文分析](/audio-paper-digest-blog/posts/iclr2026-summary/)
